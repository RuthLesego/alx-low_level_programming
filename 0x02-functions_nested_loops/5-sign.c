#include "main.h"

/**
 * main - Entry point
 * print_sign - function that prints the sign of a number
 * Return: Always 0 (success)
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		_putchar(a);
	_putchar('\n');
	return (0);
}
